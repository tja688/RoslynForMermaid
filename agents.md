# 第三阶段改造优化计划 v2 # 

---

## 0) 已确认的决策（写进规格，避免反复）

1. **Tree View 只做“当前 Snapshot 的 L0/L1/L2”** 三层入口（不做更细粒度的 feature/class 树）。
2. Mermaid 页左侧做 **两个下拉框**：

   * 上：Project Selector
   * 下：Snapshot Selector（随 Project 联动刷新可选项）
3. **进度条做阶段型**（纯前端体验优化），暂不增加后端 progress / SSE 等复杂度。
4. 右侧 Inspector 删除；节点信息改为 **画布右上角无框文字信息区**；必须落实 hover/click 交互反馈；拖拽抓取与点击冲突要解决——**采用“中键按住拖拽画布”**作为主方案（左键专注选择/点击）。

---

## 1) 总体改造目标

把当前“一个页面塞所有东西导致纵向超长”的工作台，拆成**两个选项卡页面**：

* **Tab A：配置/扫描（默认打开）**
  只做配置 + 一键扫描 + 阶段进度展示。目标：**页面不被撑高**、无需整页滚轮。
* **Tab B：Mermaid 图（扫描后自动跳转）**
  左侧 Tree View + 双下拉（Project/Snapshot），右侧超大画布。目标：**画布为核心视觉**，不再有右侧 Inspector，且**页面不随内容变长**。

---

## 2) Tab A：配置/扫描页（默认页）

### 2.1 页面结构

* 顶部：必要的“数据源/项目注册信息”等简短信息（不放任何“当前展示 Mermaid 的项目选择”）。
* 中部：配置项（字段含义不变，仅重排展示）
* 底部：**大号 Start Scan 按钮（主 CTA）** + **阶段型进度条/状态区**

### 2.2 关键交互

* 页面初始化 **不触发扫描**。
* Start Scan 点击后：

  * 进入“扫描中”阶段展示（阶段条 + 状态文案）
  * 扫描成功：自动切换到 **Tab B Mermaid 图页**，并默认打开该 Snapshot 的 **L0**。

### 2.3 阶段型进度条（前端实现）

不依赖后端真实百分比，建议阶段如下（可根据你们现有日志/状态细化）：

1. Preparing（读取配置、初始化）
2. Scanning（执行扫描）
3. Building Diagrams（生成 L0/L1/L2）
4. Saving Snapshot（写入本地快照/缓存）
5. Done / Failed

> 重点：让用户“知道系统在干嘛”，而不是转圈等死。

---

## 3) Tab B：Mermaid 图页（核心工作台）

### 3.1 布局（强约束：禁止把页面拉长）

* 整页采用固定视口工作台：`height: 100vh; overflow: hidden;`
* 左侧：Tree View 面板（独立滚动容器）
* 右侧：Mermaid 画布（最大区域）
* 删除：右侧 Inspector 栏（完全移除）

### 3.2 左侧栏内容（从上到下）

1. **Project 下拉框**
2. **Snapshot 下拉框（联动更新）**
3. （可选小工具条）Home(L0) / Back / Forward 等（如果你还要保留导航能力，就放这里）
4. **Tree View：仅三类入口**

   * L0
   * L1
   * L2
     点击任意项 -> 加载并展示对应 Mermaid 图。

> Tree View “只做当前 Snapshot 的 L0/L1/L2”，所以它本质上就是一个很干净的导航面板 + 未来可扩展容器。

### 3.3 Tree View 的滚动与可拓宽

* Tree View 内容超长时：**只滚 Tree View 本身**，不能撑高页面。
* 支持拖拽拓宽（resizable），并设置限位：

  * min 宽度：保证不至于挤没内容
  * max 宽度：防止用户把画布挤到不可用
* Tree View 顶部（两个下拉框区域）随宽度一起伸缩，不能出现“上面窄下面宽”的割裂。

---

## 4) 节点交互体验落地（hover / 可点击 / 信息展示）

你提到现在“几乎没有 hover tooltip，也没明确的可点击反馈”，这是 P0 体验问题，这一版直接写成强制验收项。

### 4.1 画布右上角：无框文字信息区（替代 Inspector）

* 常驻一个小的文本区域（不遮挡主图）展示：

  * 当前 hover 的 Node 名称（没有 hover 则显示 “Hover a node”/空）
  * 当前选中的 Node 名称（点击后锁定）
  * （可选）Node 类型/所属层（别堆太多）
* 形式：纯文字、无边框、轻量、可淡入淡出。

### 4.2 hover/click 的视觉反馈（必须有）

* hover 到可交互节点：节点**轻微变色/描边加深/亮度提高**（小幅即可）
* 点击：做明确、快速的点击反馈（与 hover 态区分），不长久保持，一闪而过即可
* 鼠标移出：hover 态消失。

### 4.3 解决“抓手拖动画布”覆盖点击的问题（你给的两方案里我选定方案 2）

**主方案：拖拽改为鼠标中键按住拖动（Pan）**

* 左键：专注 hover / click selection
* 中键按住：拖动画布
* 滚轮：缩放（保留现有）
* 额外兼容（推荐）：按住 Space + 左键拖动也可 pan（照顾无中键/触控板用户，但不强制）

> 这样能彻底避免“左键既要点又要拖”的冲突，体验也更符合图编辑器/地图类交互习惯。

---

## 5) 功能迁移清单（明确“从哪删、挪到哪”）

* **从配置页移除**：任何“当前 Mermaid 展示的项目/快照选择”相关 UI
  → 全部迁移到 Mermaid 图页左侧的双下拉。
* **删除**：右侧 Inspector（Selection details and context）整栏
  → 用“右上角无框文字信息区”替代最小信息显示。
* **替换**：画布上方简陋按钮导航
  → 导航能力（若保留）迁移到 Mermaid 左侧栏顶部（或融入 Tree 区域）。

---

## 6) 迭代拆分（按最小可用交付）

### Iteration 1：止血版布局（当天就该能看出“正常网页”）

* 引入 Tabs：Config / Mermaid
* Config 默认页隐藏画布与 Inspector
* Mermaid 页删除 Inspector，画布区域撑满右侧，整页 100vh + overflow 控制生效

### Iteration 2：扫描体验与自动跳转

* Start Scan 大按钮 + 阶段进度条
* 扫描成功自动跳 Mermaid 页，默认 L0

### Iteration 3：双下拉 + Tree View（L0/L1/L2）

* Project 下拉切换联动 Snapshot 下拉
* Tree View 三层入口跳图
* 左侧可拖拽拓宽 + 独立滚动

### Iteration 4：交互体验 P0（hover/click/信息区/拖拽冲突）

* hover/selected 高亮/交互反馈落地
* 右上角信息区落地
* Pan 改中键（可选加 Space+左键）

---

## 7) 验收标准

* 默认进入 Config 页，页面**不再“厕纸式”超长**。
* Config 页能完成配置 + 一键扫描 + 阶段进度展示；扫描成功自动切 Mermaid 页。
* Mermaid 页：

  * 左侧双下拉（Project/Snapshot）联动正确
  * Tree View 仅 L0/L1/L2，点击即跳对应图
  * 无右侧 Inspector
  * 画布占主区域，页面不随内容变高；Tree 独立滚动；可限位拓宽
* 交互体验：

  * 节点 hover 有明显但克制的视觉反馈
  * 点击选中有保持态
  * 右上角信息区能即时显示 hover/selected 的 node 信息
  * 拖动画布使用中键按住（左键不再被“抓取拖拽”抢走）
